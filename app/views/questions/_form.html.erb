<%= form_for([@question.question_set, @question]) do |f| %>
  <% if @question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question.errors.count, "error") %> prohibited this question_set from being saved:</h2>

      <ul>
      <% @question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
     <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit "Create Question" %>
  </div>
<% end %>
